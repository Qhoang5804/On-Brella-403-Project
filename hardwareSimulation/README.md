# Hardware simulation

Mock umbrella station API (Mockoon). Config: `hardware-mock.json`.

**Start mock:** `npm start` — serves on http://localhost:3000  
**Run tests:** Start the mock, then in another terminal run `npm test`
