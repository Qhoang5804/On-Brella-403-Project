{
  "name": "on-brella-hardware-mock",
  "version": "1.0.0",
  "description": "Hardware simulation mock for On-Brella",
  "scripts": {
    "start": "npx @mockoon/cli start --data hardware-mock.json --port 3000",
    "test": "node tests/hardware-mock.test.js"
  },
  "devDependencies": {
    "@mockoon/cli": "^7.0.0"
  }
}
