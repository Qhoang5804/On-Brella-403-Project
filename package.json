{
  "name": "on-brella-403-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start --prefix backend",
    "test": "npm run test --prefix frontend && npm run test --prefix backend",
    "test:node": "npm run test --prefix frontend && npm run test --prefix backend",
    "test:frontend": "npm run test --prefix frontend",
    "test:backend": "npm run test --prefix backend",
    "test:hardware": "npm run test --prefix hardwareSimulation"
  }
}
